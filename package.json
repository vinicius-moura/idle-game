{
  "name": "idle",
  "version": "1.0.0",
  "main": "script.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "serve": "nodemon --watch js --watch css --watch index.html --exec \"http-server -p 8080 -c-1\"",
    "sass": "npx sass style.scss style.css --watch",
    "dev": "concurrently \"npm run serve\" \"npm run sass\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "concurrently": "^9.2.0",
    "sass": "^1.89.2"
  }
}